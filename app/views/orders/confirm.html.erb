<div class="flex items-start justify-center w-full h-screen pt-10 bg-gray-200">
  <div class="w-screen max-w-sm px-10 py-8 bg-white shadow-md rounded-xl">
    <% if @response.status === "SUCCESS" %>

      <table>
        <tr>
          <h1>感謝您讓飛天貓飛高高</h1>
          <td class= "block mb-1 font-semibold text-gray-600">捐款人<%= @username ,class: "w-full px-4 py-2 rounded-md outline-none bg-indigo-50"%></td>
          <td class= "block mb-1 font-semibold text-gray-600">捐款編號<%= @response.order_no ,class: "w-full px-4 py-2 rounded-md outline-none bg-indigo-50"%></td>
          <td class= "block mb-1 font-semibold text-gray-600">付款時間<%= @response.pay_at ,class: "w-full px-4 py-2 rounded-md outline-none bg-indigo-50"%></td>
          <td class= "block mb-1 font-semibold text-gray-600">卡號後四碼<%= @response.card_last_4_no ,class: "w-full px-4 py-2 rounded-md outline-none bg-indigo-50"%></td>
        <tr>
      </table>
    <% else %>  

      <h1>資助失敗，請再試一次</h1>

    <% end %>
  </div>
</div>